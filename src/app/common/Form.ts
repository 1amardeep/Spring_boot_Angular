export interface IState {
  state: String
}

export interface IStateResponse {
    name: String,
    id: number
  }

  export interface ICountryResponse {
    name: String,
    id: number,
    code: String
  }